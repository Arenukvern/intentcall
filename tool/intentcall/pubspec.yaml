name: intentcall_workspace_tool
description: CLI tool for IntentCall repository maintenance.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.12.0 <4.0.0"

resolution: workspace

dependencies:
  args: ^2.7.0
  intentcall_platform:
  meta: ^1.17.0
  path: ^1.9.1

dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.1
  xsoulspace_lints: ^0.1.2
