name: dart_acp_toolkit
description: >-
  Standalone Agent Client Protocol (ACP) server library — JSON-RPC 2.0 over
  stdio with pluggable agent backends. Pure Dart.
version: 0.1.0-dev.1
license: MIT
repository: https://github.com/Arenukvern/mcp_flutter/tree/main/packages/acp_server
issue_tracker: https://github.com/Arenukvern/mcp_flutter/issues
topics:
  - acp
  - agent
  - jsonrpc
  - stdio
  - ide
environment:
  sdk: ">=3.12.0 <4.0.0"
resolution: workspace
dependencies:
  meta: ^1.18.0
  from_json_to_json: ^0.5.0
dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.0
