# Flutter plugin — app-level lint set.
include: package:xsoulspace_lints/app.yaml

analyzer:
  language:
    strict-casts: true
  errors:
    lines_longer_than_80_chars: ignore
  exclude:
    - "**/*.g.dart"
