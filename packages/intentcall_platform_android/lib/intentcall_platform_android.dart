/// Native-only Android federated implementation for `intentcall_platform`.
///
/// Dart API lives in the umbrella `intentcall_platform` package. This library
/// exists so the package has a valid Dart entrypoint for pub.
library;
