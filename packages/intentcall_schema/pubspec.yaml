name: intentcall_schema
description: >-
  PRE-RELEASE — Wire contract for IntentCall: AgentResult envelopes, JSON Schema
  validation, entity snapshots, and VM service wire parsing. Pure Dart.
version: 0.6.0
license: MIT
repository: https://github.com/Arenukvern/intentcall/tree/main/packages/intentcall_schema
issue_tracker: https://github.com/Arenukvern/intentcall/issues
homepage: https://github.com/Arenukvern/intentcall/tree/main/packages/intentcall_schema
topics:
  - mcp
  - agents
  - dart

environment:
  sdk: ">=3.12.0 <4.0.0"
resolution: workspace

dependencies:
  meta: ^1.17.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.1
  xsoulspace_lints: ^0.1.2
