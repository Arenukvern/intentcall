name: intentcall_workspace
publish_to: none
description: Standalone intentcall monorepo

environment:
  sdk: ^3.12.0

workspace:
  - packages/*
  - tool/intentcall

dev_dependencies:
  lints: ^6.1.0
  xsoulspace_lints: ^0.1.2
